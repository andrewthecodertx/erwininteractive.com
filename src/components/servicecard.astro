---
const { background, title, description, icon, color } = Astro.props;

---

<div class:list={[background, "p-8 rounded-lg shadow-lg shadow-gray-500 transform transition hover:-translate-y-2"]}>
  <div class:list={[color, "mb-4"]}>
    <i class:list={["fas", icon, "text-4xl"]}></i>
  </div>
  <h3 class:list={["text-2xl font-bold mb-4", color]}>{title}</h3>
  <p class:list={[color]}>{description}</p>
</div>
